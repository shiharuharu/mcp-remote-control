"""mcp-remote-control: remote host control for agents via MCP and CLI.

Same Core ops power the MCP tools and the harness CLI. Configure under
``MRC_HOME`` (profiles, secrets); open endpoints; then exec, fs, screen,
or ps. Serial hardware uses the separate console tool.
"""

__version__ = "0.2.1"
