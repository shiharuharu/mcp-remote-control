# Fixture global config for unit / doctor / selftest.

[defaults]
verbosity = "normal"
max_body_chars = 24000

[security]
# Removed legacy knobs; still listed so load proves it ignores unknown keys.
redact_secrets_in_logs = true
allow_secret_paths_in_output = true
